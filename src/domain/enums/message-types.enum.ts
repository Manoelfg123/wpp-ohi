/**
 * Enum que representa os tipos de mensagens suportados pelo WhatsApp
 */
export enum MessageType {
  TEXT = 'text',
  IMAGE = 'image',
  VIDEO = 'video',
  AUDIO = 'audio',
  DOCUMENT = 'document',
  LOCATION = 'location',
  CONTACT = 'contact',
  BUTTONS = 'buttons',
  LIST = 'list',
  TEMPLATE = 'template',
  REACTION = 'reaction',
  STICKER = 'sticker',
  STORY = 'story'
}
